{
  "name": "app-base",
  "version": "1.0.0",
  "description": "Base para criação de aplicativos .exe com compatibilidade ideal",
  "main": "src/electron/index.js",
  "scripts": {
    "electron:dev": "electron .",
    "electron:build:windows": "electron-packager . app-base --platform=win32 --arch=x64 --out=dist --icon=icon.ico --overwrite --prune=true --asar=true"
  },
  "keywords": [
    "app",
    "desktop",
    "electron",
    "electron-app"
  ],
  "author": "Israel R. Jatobá",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "electron": "^36.2.1",
    "electron-packager": "^17.1.2",
    "tailwindcss": "^4.1.7",
    "wait-on": "^8.0.3"
  },
  "dependencies": {
    "consola": "^3.4.2",
    "dotenv": "^16.5.0",
    "ejs": "^3.1.10",
    "express": "^5.1.0"
  }
}
